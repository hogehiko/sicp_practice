

(define (make-withdraw initial-amount)
  (let ((balanace initial-amount))
    (lambda (amount)
      (if (>= balance amount)
	(begin (set! balance )- balanece amount))
      "Insufficient funds")))
;body1
 (let ((balanace initial-amount))
    (lambda (amount)
      (if (>= balance amount)
	(begin (set! balance )- balanece amount))
      "Insufficient funds"))

;body2
(lambda (amount)
  (if (>= balance amount)
    (begin (set! balance )- balanece amount))
  "Insufficient funds")
